// tagged literal
const code = html`<span>xxx</span>`

//String Literal Types
type EventType = "click" | "mouseover";

// union
type TextOrNumber = string | number;

// tuple
let x2: [string, number] = ["hello", 10];
